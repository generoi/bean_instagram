<?php

/**
 * @file 
 * Contains tests for the Instagram Bean module.
 */

/**
 * Test case.
 */
class BeanInstagramTestCase extends DrupalWebTestCase {

  /**
   * Implements getInfo().
   */
  public static function getInfo() {
    return array(
      'name' => t('Instagram Bean tests'),
      'description' => t('TODO: write me.'),
      'group' => t('Instagram Bean'),
    );
  }

  /**
   * Implements setUp().
   */
  function setUp() {
    // Call the parent with an array of modules to enable for the test.
    parent::setUp(array('bean_instagram'));

    // TODO: perform additional setup tasks here if required.
  }

  /**
   * Test the module's functionality.
   */
  function testTodoChangeThisName() {
    // TODO: write test!
  }

}